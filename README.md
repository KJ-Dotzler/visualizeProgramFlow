# visualizeProgramFlow
Visual representation of simple for loop in programming, tracks variable values and output values through each iteration.
Update the HTML to not include comparator input, fix JS functions that use comparator input as well.